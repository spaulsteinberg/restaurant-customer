import ILink from "../../models/home/ILink"

type HomeLinksProps = {
    links: ILink[]
}

const HomeLinks = ({ links }: HomeLinksProps) => {
    return (
        <div>

        </div>
    )
}

export default HomeLinks
